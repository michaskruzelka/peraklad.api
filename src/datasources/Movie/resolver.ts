const resolver = {
    Query: {
        movieByImdbId: () => {
            return {};
        },
        movieByTitleAndYear: () => {
            return {};
        },
    },
};

export default resolver;
