import { Locale } from './types';

const DEFAULT_LOCALE = Locale.BE;
const DEFAULT_LANG = 'en';

const PRIMARY_LANGS = ['en', 'ru', 'pl', 'de', 'fr', 'ko'];

export { DEFAULT_LOCALE, PRIMARY_LANGS, DEFAULT_LANG };
