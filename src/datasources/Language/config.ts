import { Locale } from './types';

const DEFAULT_LOCALE = Locale.BE;
const DEFAULT_LANG = 'en';

const PRIMARY_LANGS = ['en', 'ru', 'pl', 'de', 'fr', 'es', 'it'];
const RECOMMENDATION_LANGS = ['ru', 'uk'];

export { DEFAULT_LOCALE, PRIMARY_LANGS, DEFAULT_LANG, RECOMMENDATION_LANGS };
